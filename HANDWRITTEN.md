# Handwritten Residue Beside Generated OCEL Code

The OCEL 2.0 event model, mapper, serializer, hash chain, receipt chain, loop state machine and
schemas in `src/generated/` are projections of `ontology/zcode-loop.ttl` through the extended
marketplace packs (`bun scripts/gen-ocel.ts`). Nothing in `src/generated/` is edited by hand
(`bun scripts/gen-ocel.ts --check` fails when it drifts). This file lists the irreducible handwritten
residue and why each row cannot be generated yet.

## Residue

| File | Role | Status | Why not generated |
| --- | --- | --- | --- |
| `src/ocel-tap.ts` | Launcher-level tap: reads `-p --output-format stream-json` / `app-server` stdout, feeds the generated `Tap`, writes `<session>.jsonocel` and `<session>.receipt.json`, gated by `ZCODE_OCEL=1`, directory `ZCODE_OCEL_DIR` (default `~/.zcode/ocel`) | UNSUPPORTED (generator-capability) | No pack projects a process-attached sink hook (spawn, tee, session file naming, epoch-ms to ISO 8601 normalisation). The process-intelligence pack emits the adapter and mapper only |
| `src/launcher.ts` (hook in `runRuntime`) | Pipes child stdout through `OcelRecorder` when the tap is enabled | UNSUPPORTED (generator-capability) | Launcher control flow is consumer code |
| `scripts/gen-ocel.ts` | Runs `ggen sync run` and byte-copies pack outputs to `src/generated/` | UNSUPPORTED (generator-capability) | Pack templates fix their own output paths (`src/pi_ocel_tap/...`); the consumer relocates them, it never edits them |
| `scripts/gen-zcode-loop.ts`, `scripts/zcode-events.ts` | Draft of `ontology/zcode-loop.ttl` from the runtime's enums in `vendor/zcode.cjs` | DRAFT | The wire-to-object mapping table is authored judgement; the ontology is the reviewed source after the first run |
| `test/ocel-*.test.ts`, `test/support/ocel.ts` | Contract, conformance and reuse tests | Handwritten by design | Tests are the falsifiers for the generated code |
| `src/gall-work.ts` (+ `test/gall-work.test.ts`, `test/runtime/gall-work-e2e.test.ts`, `src/launcher.ts` dispatch hook) | Native gall-work lease lifecycle client (claim/persist/construct/close) against the xaas execution fabric; ported from `w9-sweep/zcode-pr3@454ed62` onto main 2026-09-22 | UNSUPPORTED (generator-capability) | No pack projects a lease-protocol client; the contract fixture `test/fixtures/gall-work.contract.json` is sha256-pinned byte-identical to `~/xaas/priv/zcode_plugin/gall-work.contract.json` |
| `src/relay-admission.ts` (+ `test/relay-admission.test.ts`, `test/fixtures/xaas-remote-relay.contract.json`) | Worker-side admission for `xaas-remote-relay/1`: runs the contract `admission_order` over envelopes bound to `gall.work-lease/1`, emits the 14 typed refusals plus `KNOWN_REPLAY`, requires an explicit local allow-do (`ZCODE_RELAY_ALLOW_DO=1` / `allowDo: true`) beyond `authority_ref` for `verb=actuate`, and keeps durable ack state (`zcode.relay-ack-state/2`) in `<tmpdir>/xaas-fabric/<sha256(worktree\nepoch)>.relay-ack.json` so a restart before ACK keeps the pending `command_id`; every acknowledged command id digest is kept for the epoch (replay is not windowed by `dedupLimit`), transitions re-read state under an O_EXCL `<state>.lock` (dead-pid locks are stale), pending retries must match channel and `authority_ref`, non-canonical `actuate` spellings are `INVALID_ENVELOPE`, and dispatch reports a refused ACK as `unacknowledged`, not `executed`; the contract fixture is byte-identical to xaas `priv/ultracode/remote-relay.contract.json` at 9820fe3b (sha256-pinned); added 2026-09-25 (v26.9.26) | UNSUPPORTED (generator-capability) | No pack projects a relay-admission court or a durable ack ledger; stage order and refusal names are pinned against the vendored contract by the test, so contract drift fails the test instead of silently diverging |
| `src/gall-cli.ts` (+ `src/launcher.ts` `runGallCommand` dispatch, `test/runtime/gall-fresh-consumer.test.ts`) | Public GALL-006 fresh-consumer command `zcode gall verify (--artifact FILE \| --bundle DIR)`; the launcher dispatch (first landed 9d2ccec) was dropped by a v26.9.22 merge while `src/gall-cli.ts` survived, and restored verbatim 2026-09-24 (v26.9.25 fleet lane) | UNSUPPORTED (generator-capability) | No pack projects a portable-artifact verifier CLI; restoration is conservation of an admitted capability, not new logic |
| `src/evidence-cli.ts` (+ `test/evidence-cli.test.ts`, `src/launcher.ts` dispatch hook) | Deterministic PolyEvidence admission court: validates exact-subject typed evidence and requires a replayable evidence → constraint → hypothesis → verification → construction path; emits proof-only receipts with `authority=none` and `external_do_count=0` | UNSUPPORTED (generator-capability) | No marketplace pack yet projects the portable evidence-bundle verifier/CLI; this is the irreducible consumer boundary until the PolyEvidence pack exists |
| `scripts/validate-receipts.ts` | Wraps `~/.claude/dfcm/validate_receipt.py` over `receipts/**/*.json`, fail-closed on zero files or any refusal | UNSUPPORTED (generator-capability) | No pack projects receipt validation over the dfcm schema; added 2026-09-22 |
| `src/session-model-recovery.ts` (+ `test/session-model-recovery.test.ts`) | Recovers invalid saved model selections (provider-not-found, reasoning-level, selection-missing) with issue guidance; v26.9.23 L4 added the bare-family-key registry-repair guidance | UNSUPPORTED (generator-capability) | No pack projects session-state diagnosis or provider-registry repair guidance; file originated upstream 8cdc0de 2026-09-20, handwritten lines first ledgered 2026-09-23 (v26.9.23 wave) |
| `src/execution-providers.ts` (+ `test/execution-providers.test.ts`) | Execution-provider registry: `~/.zcode/v2/execution_provider_config.json` (schemaVersion 1, `executionProviderRules`/`defaultExecutionSelection`), fail-closed selection cut-point (`enabled: false` = unselectable; built-in zcode default when absent); gall-work resolves its wire `provider` through it (ALOOP-ZCODE-DOGFOOD-001) | UNSUPPORTED (generator-capability) | No pack projects a provider-registry read-gate; path helper imitates `src/config-paths.ts`, read-gate style imitates `src/model-access.ts`; added 2026-09-25 |
| `src/parts-cli.ts` (+ `src/launcher.ts` dispatch, `test/parts-cli.test.ts`, `test/parts-cli-hardening.test.ts`, `test/parts-cli-bench.test.ts`, `scripts/bench-parts-alternatives.ts`, `docs/bench/v26.9.26/parts-alternatives-bench.json`) | Thin consumer of admitted `unrdf.semantic-parts.v1` graphs for `zcode parts inspect|alternatives|falsify`; performs SELECT only, emits CANDIDATE/authority NONE, and never edits source or actuates a replacement. v26.9.26 hardening: total (query-independent) admission incl. source shape, unsourced parts never matched by file reference, self-candidate falsify refusal, `graph_sha256` bound into every payload/receipt + `--graph-sha256` stale-graph pin, flag-as-value refusal; seeded benchmark with a near-linear regression bound | UNSUPPORTED (generator-capability) | No pack currently projects a zcode-specific consumer CLI for the semantic-parts graph contract; matching logic is intentionally limited to exact canonical `semantic_id` intersection and refuses ambient authority |
| `scripts/workflow-toolchain-court.ts` (+ `test/release-workflows.test.ts`, `test/workflow-toolchain-court-hardening.test.ts`, `test/workflow-toolchain-court-bench.test.ts`, `test/workflow-toolchain-court-conservation.test.ts`, `scripts/bench-toolchain-court.ts`, `docs/bench/v26.9.26/toolchain-court-bench.json`) | ggen toolchain workflow court (PR #9): every job reaching a `ZCODE_REQUIRE_TOOLCHAINS=1` script must run `./.github/actions/ggen-toolchain` after checkout, before that step, unconditionally and fail-stop; the composite step itself must start `set -euo pipefail` and never relax it. v26.9.26 hardening: total script-reference tokenizer (flags around `run`, `run-script`, `npm test`, `bun <script>`/`pnpm <script>`/`yarn <script>` shorthands, quoting, direct `scripts/build-release.ts`), job-level `continue-on-error` refusal, whole-script fail-stop (`set +e`/`set +o pipefail`/`\|\| true`); seeded benchmark with a near-linear regression bound. Repair: tokenizer is a superset of the pre-hardening regex (conservation), flattens subshells/`$( )`/backticks/`sh -c`/`&`/line continuations, reads value flags both ways, quoted `ZCODE_REQUIRE_TOOLCHAINS="1"`; composite court refuses non-`bash` shells, mid-line relax, non-exiting `\|\|` fallbacks and early `exit 0`. SELECT only, authority NONE | UNSUPPORTED (generator-capability) | No pack projects a GitHub Actions workflow court; extracted verbatim from the test file so it can be benchmarked, then hardened |

## Not done here

- `vendor/zcode.cjs` is not patched. The runtime-internal hook that would see events before the wire
  projection stays out; the tap consumes the wire output instead, so it survives runtime updates as long
  as `test/ocel-generated.test.ts` (shape contract) and `test/ocel-coverage.test.ts` (enum drift) pass.
- The shacl pack's zod target is not committed (ZOCEL-006 decision): nothing in the repo imports it, `zod`
  is not a dependency, and `src/generated/schemas.json` (JSON Schema 2020-12 from the same shapes) is the
  validator the tests exercise. `scripts/gen-ocel.ts` omits it from RELOCATION.
- The app-server subscription paths (`params.*`) are UNVERIFIED: no live subscription was captured; the
  test uses a synthetic envelope around real wire events.

## Fresh-clone pack root

`bun scripts/gen-ocel.ts` (and `--check`) needs the four marketplace packs (process-intelligence,
state-transition, evidence-standing, shacl-projection) and the `ggen` binary. The default pack directory is
`/Users/sac/ggen-marketplace/packs`. On any other machine or worktree set `ZCODE_PACK_ROOT=<packs dir>`;
`ZCODE_PACK_ROOT_PI|ST|ES|SHACL` override a single pack's parent dir. Tested by
`test/ocel-reuse.test.ts` (path resolution) and by running
`ZCODE_PACK_ROOT=$HOME/ggen-marketplace/packs bun scripts/gen-ocel.ts --check` in a fresh clone (ZOCEL-006 receipt).
