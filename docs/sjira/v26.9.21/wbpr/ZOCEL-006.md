
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-006. DO NOT EDIT. -->
# WBPR: ZOCEL-006 — Consumer residue hardening

Close the residual consumer risks: decide the zod dependency so the zod schemas typecheck, record a contract fixture from a real runtime turn, review the loop ontology, and define how a fresh clone finds the pack root.

## Work order boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** 5e20ab3c9dff97edd35deee6759c5e5961ee1a05
- **Exact subject:** zcode-cli:consumer-residue-hardening
- **Standing:** UNKNOWN
- **Evidence ceiling:** IMPLEMENTED_UNVERIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.21:ZOCEL-006
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-006

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Work breakdown structure

One breakdown unit per sj:acceptance criterion, numbered by sorted target IRI.
The canonical graph attaches sj:falsifier, sj:nextCheckpoint, and sj:nextAction at
WorkOrder scope; this projection repeats them per unit without inventing
per-requirement edges.

### WBS 1 — Zod decision typechecks

- **Requirement:** **Zod decision typechecks** — cd /Users/sac/dev/zcode-cli && bun run typecheck; echo rc=$?. Expected: rc=0 with the zod decision recorded (zod in package.json dependencies or schemas.zod.ts removed). Earns: TYPECHECK_OK.

- **Falsifier:** **Hand-written fixture** — A fixture not captured from a real runtime turn falsifies the contract claim.

- **Falsifier:** **Insensitive fixture** — A shape change that leaves the fixture test green falsifies fixture sensitivity.


- **Closure checkpoint:** **ZOCEL-006 checkpoint** — Closed when the ZOCEL-006 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-006** — Run the acceptance commands of ZOCEL-006 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 2 — Real-turn contract fixture

- **Requirement:** **Real-turn contract fixture** — cd /Users/sac/dev/zcode-cli && test -s test/fixtures/runtime-turn.contract.json && bun test test/ocel-launcher.test.ts exits 0; then in a copy of the fixture rename one event field with sed -i.bak 's/"type"/"kind"/' and rerun the test against the copy: exit nonzero. Both required. Earns: CONTRACT_SENSITIVE.

- **Falsifier:** **Hand-written fixture** — A fixture not captured from a real runtime turn falsifies the contract claim.

- **Falsifier:** **Insensitive fixture** — A shape change that leaves the fixture test green falsifies fixture sensitivity.


- **Closure checkpoint:** **ZOCEL-006 checkpoint** — Closed when the ZOCEL-006 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-006** — Run the acceptance commands of ZOCEL-006 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 3 — Ontology reviewed

- **Requirement:** **Ontology reviewed** — cd /Users/sac/dev/zcode-cli && python3 -c "import rdflib;g=rdflib.Graph();g.parse('ontology/zcode-loop.ttl');print(len(g))"; echo rc=$?. Expected: rc=0 and a triple count > 0, plus a review-outcome line in the ZOCEL-006 receipt. Earns: ONTOLOGY_PARSES.

- **Falsifier:** **Hand-written fixture** — A fixture not captured from a real runtime turn falsifies the contract claim.

- **Falsifier:** **Insensitive fixture** — A shape change that leaves the fixture test green falsifies fixture sensitivity.


- **Closure checkpoint:** **ZOCEL-006 checkpoint** — Closed when the ZOCEL-006 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-006** — Run the acceptance commands of ZOCEL-006 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 4 — Fresh-clone pack root

- **Requirement:** **Fresh-clone pack root** — mkdir -p /Users/sac/wt/zocel-runs && rm -rf /Users/sac/wt/zocel-runs/fresh && git clone --branch $(git -C /Users/sac/wt/zcode-ocel-consumer branch --show-current) /Users/sac/wt/zcode-ocel-consumer /Users/sac/wt/zocel-runs/fresh && cd /Users/sac/wt/zocel-runs/fresh && bun install && bun scripts/gen-ocel.ts --check; echo rc=$?. Expected: rc=0 with no STALE line. Earns: PACK_ROOT_PORTABLE.

- **Falsifier:** **Hand-written fixture** — A fixture not captured from a real runtime turn falsifies the contract claim.

- **Falsifier:** **Insensitive fixture** — A shape change that leaves the fixture test green falsifies fixture sensitivity.


- **Closure checkpoint:** **ZOCEL-006 checkpoint** — Closed when the ZOCEL-006 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-006** — Run the acceptance commands of ZOCEL-006 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



## Breakdown tally

- Requirements: 4
- Falsifiers: 2
- Closure checkpoints: 1
- Next actions: 1

## Court spine

Required courts:

- **ZOCEL-006 exact-subject court** — Independent court for Consumer residue hardening: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


Required evidence:

- **Local execution evidence** — Observed repository-local exact-subject execution evidence.

- **Receipt evidence** — Durable receipt evidence for the exact subject and transition.

- **Runtime evidence** — Runtime execution evidence for the exact admitted subject.

- **Source evidence** — Exact canonical-source identity evidence.


Required receipt classes:


- verification



Authority requirement:


- NONE



## Promotion rule

Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

## Path scope


- ontology/zcode-loop.ttl

- schemas.zod.ts


