
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-011. DO NOT EDIT. -->
# WBPR: ZOCEL-011 — Retire unverified follow-ups

Resolve the UNVERIFIED follow-ups: mark the adapter ALIVE only after an observed real run, add a second real consumer over the xaas Ultracode loop graph with a comparison query, and prove Python reuse.

## Work order boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** 5e20ab3c9dff97edd35deee6759c5e5961ee1a05
- **Exact subject:** zcode-cli:ocel-followups
- **Standing:** UNKNOWN
- **Evidence ceiling:** SPECIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.21:ZOCEL-011
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-011

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Work breakdown structure

One breakdown unit per sj:acceptance criterion, numbered by sorted target IRI.
The canonical graph attaches sj:falsifier, sj:nextCheckpoint, and sj:nextAction at
WorkOrder scope; this projection repeats them per unit without inventing
per-requirement edges.

### WBS 1 — Adapter ALIVE by observation

- **Requirement:** **Adapter ALIVE by observation** — cd /Users/sac/dev/zcode-cli && the ZOCEL-001 verifier command (acc-zocel-001-4) exits 0 on a real "$LOG"; adapter standing is ALIVE only if that exit is 0, otherwise NOT_RUN. Earns: ADAPTER_ALIVE.

- **Falsifier:** **Adapter claimed without run** — An ALIVE mark with no real run log falsifies the adapter claim.

- **Falsifier:** **Empty comparison** — A compare-loops query returning zero rows falsifies comparability.


- **Closure checkpoint:** **ZOCEL-011 checkpoint** — Closed when the ZOCEL-011 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-011** — Run the acceptance commands of ZOCEL-011 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 2 — Second consumer

- **Requirement:** **Second consumer** — python3 -c "import json,jsonschema,sys; jsonschema.validate(json.load(open(sys.argv[1])), json.load(open('/Users/sac/gymact/src/gymact/schemas/ocel20-schema.json')))" the xaas Ultracode loop-graph OCEL file (find /Users/sac/xaas -name '*.jsonocel' -o -name '*ocel*.json' | head -1); expected: exit 0, no output. No such file => BLOCKED. Earns: SECOND_CONSUMER_VALID.

- **Falsifier:** **Adapter claimed without run** — An ALIVE mark with no real run log falsifies the adapter claim.

- **Falsifier:** **Empty comparison** — A compare-loops query returning zero rows falsifies comparability.


- **Closure checkpoint:** **ZOCEL-011 checkpoint** — Closed when the ZOCEL-011 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-011** — Run the acceptance commands of ZOCEL-011 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 3 — Loop comparison query

- **Requirement:** **Loop comparison query** — test -s /Users/sac/dev/zcode-cli/queries/compare-loops.rq || echo NOT_RUN (query authored by this work order; it does not exist on main or in pi-ocel-tap). cd /Users/sac/dev/zcode-cli && python3 -c "import rdflib,sys;g=rdflib.Graph();[g.parse(f) for f in sys.argv[1:]];print(len(list(g.query(open('queries/compare-loops.rq').read()))))" /Users/sac/wt/zocel-runs/zcode.ttl /Users/sac/wt/zocel-runs/ultracode.ttl (the two consumers' logs converted to RDF); expected: exit 0 and printed row count >= 1. Zero rows => FAIL. Earns: LOOPS_COMPARABLE.

- **Falsifier:** **Adapter claimed without run** — An ALIVE mark with no real run log falsifies the adapter claim.

- **Falsifier:** **Empty comparison** — A compare-loops query returning zero rows falsifies comparability.


- **Closure checkpoint:** **ZOCEL-011 checkpoint** — Closed when the ZOCEL-011 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-011** — Run the acceptance commands of ZOCEL-011 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 4 — Python reuse proof

- **Requirement:** **Python reuse proof** — test -d /Users/sac/dev/zcode-cli/test/py || echo NOT_RUN (created under ZOCEL-002). cd /Users/sac/dev/zcode-cli && python3 -m pytest -q test/py 2>&1 | tail -2; expected exit 0 with '0 failed', using the shared golden vector test/fixtures/ocel-golden-chain.json. Earns: PY_REUSE.

- **Falsifier:** **Adapter claimed without run** — An ALIVE mark with no real run log falsifies the adapter claim.

- **Falsifier:** **Empty comparison** — A compare-loops query returning zero rows falsifies comparability.


- **Closure checkpoint:** **ZOCEL-011 checkpoint** — Closed when the ZOCEL-011 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-011** — Run the acceptance commands of ZOCEL-011 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



## Breakdown tally

- Requirements: 4
- Falsifiers: 2
- Closure checkpoints: 1
- Next actions: 1

## Court spine

Required courts:

- **ZOCEL-011 exact-subject court** — Independent court for Retire unverified follow-ups: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


Required evidence:

- **Process evidence** — Observed process evidence kept separate from normative specification.

- **Receipt evidence** — Durable receipt evidence for the exact subject and transition.

- **Runtime evidence** — Runtime execution evidence for the exact admitted subject.

- **Source evidence** — Exact canonical-source identity evidence.


Required receipt classes:


- replay

- verification



Authority requirement:


- NONE



## Promotion rule

Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

## Path scope


- ontology

- queries

- scripts

- test


