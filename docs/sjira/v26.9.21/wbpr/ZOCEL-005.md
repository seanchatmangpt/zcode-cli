
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-005. DO NOT EDIT. -->
# WBPR: ZOCEL-005 — Coverage gate, conformance replay, and importer cross-validation

Add an OCEL coverage gate and conformance replay, and cross-validate a real run's OCEL log by importing it with an independent process-mining importer.

## Work order boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** 5e20ab3c9dff97edd35deee6759c5e5961ee1a05
- **Exact subject:** zcode-cli:ocel-coverage-conformance-import
- **Standing:** UNKNOWN
- **Evidence ceiling:** IMPLEMENTED_UNVERIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.21:ZOCEL-005
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-005

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Work breakdown structure

One breakdown unit per sj:acceptance criterion, numbered by sorted target IRI.
The canonical graph attaches sj:falsifier, sj:nextCheckpoint, and sj:nextAction at
WorkOrder scope; this projection repeats them per unit without inventing
per-requirement edges.

### WBS 1 — Coverage gate enforced

- **Requirement:** **Coverage gate enforced** — mkdir -p /Users/sac/wt/zocel-runs && cd /Users/sac/dev/zcode-cli && bun test test/ocel-coverage.test.ts exits 0 over the real log "$LOG"; then delete one declared event type's lines from a copy /Users/sac/wt/zocel-runs/cov-copy.jsonocel and rerun against the copy: exit nonzero. Both outcomes required. Earns: COVERAGE_ENFORCED.

- **Falsifier:** **Synthetic log only** — An importer run over a hand-written log rather than a real run falsifies cross-validation.

- **Falsifier:** **Count mismatch** — Event or object counts that differ between source and importer falsify the import claim.


- **Closure checkpoint:** **ZOCEL-005 checkpoint** — Closed when the ZOCEL-005 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-005** — Run the acceptance commands of ZOCEL-005 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 2 — Conformance replay passes

- **Requirement:** **Conformance replay passes** — cd /Users/sac/dev/zcode-cli && bun test test/ocel-conformance.test.ts exits 0 and replay of "$LOG" prints 'deviations: 0' (zero deviations). No real LOG => NOT_RUN. Earns: REPLAY_CONFORMANT.

- **Falsifier:** **Synthetic log only** — An importer run over a hand-written log rather than a real run falsifies cross-validation.

- **Falsifier:** **Count mismatch** — Event or object counts that differ between source and importer falsify the import claim.


- **Closure checkpoint:** **ZOCEL-005 checkpoint** — Closed when the ZOCEL-005 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-005** — Run the acceptance commands of ZOCEL-005 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 3 — Importer accepts the log

- **Requirement:** **Importer accepts the log** — LOG=$(ls -t /Users/sac/wt/zocel-runs/*/*.jsonocel | head -1); import "$LOG" with the ex4pm importer CLI (ls /Users/sac/ex4pm exists; /Users/sac/wasm4pm is absent, use it only if it appears); expected exit 0 and printed event and object counts equal to jq '.events|length' and jq '.objects|length' of "$LOG". No LOG or no importer => BLOCKED, not ALIVE. Earns: IMPORT_CROSS_VALIDATED.

- **Falsifier:** **Synthetic log only** — An importer run over a hand-written log rather than a real run falsifies cross-validation.

- **Falsifier:** **Count mismatch** — Event or object counts that differ between source and importer falsify the import claim.


- **Closure checkpoint:** **ZOCEL-005 checkpoint** — Closed when the ZOCEL-005 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-005** — Run the acceptance commands of ZOCEL-005 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



## Breakdown tally

- Requirements: 3
- Falsifiers: 2
- Closure checkpoints: 1
- Next actions: 1

## Court spine

Required courts:

- **ZOCEL-005 exact-subject court** — Independent court for Coverage gate, conformance replay, and importer cross-validation: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


Required evidence:

- **Local execution evidence** — Observed repository-local exact-subject execution evidence.

- **Process evidence** — Observed process evidence kept separate from normative specification.

- **Replay evidence** — Fresh replay over pack, dependency, graph, consequence, toolchain, and environment identities.

- **Runtime evidence** — Runtime execution evidence for the exact admitted subject.

- **Source evidence** — Exact canonical-source identity evidence.


Required receipt classes:


- replay

- verification



Authority requirement:


- NONE



## Promotion rule

Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

## Path scope


- scripts

- src

- test


