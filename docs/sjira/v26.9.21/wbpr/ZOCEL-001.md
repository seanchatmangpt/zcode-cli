
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-001. DO NOT EDIT. -->
# WBPR: ZOCEL-001 — Land the OCEL consumer branch with a real observed run

Land feat/zcode-ocel-consumer so the zcode agent loop emits a sealed OCEL 2.0 log and receipt from a real session, verified by the generated verifier and replayed through the generated transition table. A run with no log is NOT_RUN per the gymact ocel-standing rule.

## Work order boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** 1aceb47e935200c6f67960fa2b6125d1a24fae85
- **Exact subject:** zcode-cli:feat/zcode-ocel-consumer
- **Standing:** UNKNOWN
- **Evidence ceiling:** IMPLEMENTED_UNVERIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.21:ZOCEL-001
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-001

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Work breakdown structure

One breakdown unit per sj:acceptance criterion, numbered by sorted target IRI.
The canonical graph attaches sj:falsifier, sj:nextCheckpoint, and sj:nextAction at
WorkOrder scope; this projection repeats them per unit without inventing
per-requirement edges.

### WBS 1 — Static gates pass

- **Requirement:** **Static gates pass** — In /Users/sac/wt/zcode-ocel-consumer, 'bun run typecheck' exits 0, 'bun test test/ocel-*.test.ts' reports 0 fail, and 'bun scripts/gen-ocel.ts --check' prints no STALE line and exits 0.

- **Falsifier:** **Fixture-only evidence** — A passing test suite with no real .jsonocel from a live zcode run falsifies ALIVE; standing stays NOT_RUN.

- **Falsifier:** **Stale generated code** — 'gen-ocel.ts --check' reporting STALE falsifies the claim that generated verifier and table match the ontology.

- **Falsifier:** **Tamper undetected** — A mutated log that still chain-verifies falsifies tamper detection.


- **Closure checkpoint:** **ZOCEL-001 checkpoint** — Closed when the ZOCEL-001 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-001** — Run the acceptance commands of ZOCEL-001 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 2 — Real run emits log and receipt

- **Requirement:** **Real run emits log and receipt** — 'ZCODE_OCEL=1 zcode -p <prompt> --output-format stream-json' exits 0 and produces <session>.jsonocel and <session>.receipt.json on disk; absence of either is NOT_RUN, never ALIVE.

- **Falsifier:** **Fixture-only evidence** — A passing test suite with no real .jsonocel from a live zcode run falsifies ALIVE; standing stays NOT_RUN.

- **Falsifier:** **Stale generated code** — 'gen-ocel.ts --check' reporting STALE falsifies the claim that generated verifier and table match the ontology.

- **Falsifier:** **Tamper undetected** — A mutated log that still chain-verifies falsifies tamper detection.


- **Closure checkpoint:** **ZOCEL-001 checkpoint** — Closed when the ZOCEL-001 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-001** — Run the acceptance commands of ZOCEL-001 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 3 — Log validates against OCEL 2.0 schema

- **Requirement:** **Log validates against OCEL 2.0 schema** — The produced .jsonocel validates against an OCEL 2.0 jsonschema with zero errors.

- **Falsifier:** **Fixture-only evidence** — A passing test suite with no real .jsonocel from a live zcode run falsifies ALIVE; standing stays NOT_RUN.

- **Falsifier:** **Stale generated code** — 'gen-ocel.ts --check' reporting STALE falsifies the claim that generated verifier and table match the ontology.

- **Falsifier:** **Tamper undetected** — A mutated log that still chain-verifies falsifies tamper detection.


- **Closure checkpoint:** **ZOCEL-001 checkpoint** — Closed when the ZOCEL-001 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-001** — Run the acceptance commands of ZOCEL-001 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 4 — Receipt chain verifies

- **Requirement:** **Receipt chain verifies** — The generated verifier chain-verifies the receipt over the real log and exits 0.

- **Falsifier:** **Fixture-only evidence** — A passing test suite with no real .jsonocel from a live zcode run falsifies ALIVE; standing stays NOT_RUN.

- **Falsifier:** **Stale generated code** — 'gen-ocel.ts --check' reporting STALE falsifies the claim that generated verifier and table match the ontology.

- **Falsifier:** **Tamper undetected** — A mutated log that still chain-verifies falsifies tamper detection.


- **Closure checkpoint:** **ZOCEL-001 checkpoint** — Closed when the ZOCEL-001 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-001** — Run the acceptance commands of ZOCEL-001 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 5 — Replay is conformant

- **Requirement:** **Replay is conformant** — Replaying the real log through the generated st: transition table yields zero non-conformant transitions.

- **Falsifier:** **Fixture-only evidence** — A passing test suite with no real .jsonocel from a live zcode run falsifies ALIVE; standing stays NOT_RUN.

- **Falsifier:** **Stale generated code** — 'gen-ocel.ts --check' reporting STALE falsifies the claim that generated verifier and table match the ontology.

- **Falsifier:** **Tamper undetected** — A mutated log that still chain-verifies falsifies tamper detection.


- **Closure checkpoint:** **ZOCEL-001 checkpoint** — Closed when the ZOCEL-001 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-001** — Run the acceptance commands of ZOCEL-001 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



### WBS 6 — Seal-twice refused and tamper detected

- **Requirement:** **Seal-twice refused and tamper detected** — Sealing the same session a second time is refused with a typed error, and flipping one byte of the log makes chain verification fail.

- **Falsifier:** **Fixture-only evidence** — A passing test suite with no real .jsonocel from a live zcode run falsifies ALIVE; standing stays NOT_RUN.

- **Falsifier:** **Stale generated code** — 'gen-ocel.ts --check' reporting STALE falsifies the claim that generated verifier and table match the ontology.

- **Falsifier:** **Tamper undetected** — A mutated log that still chain-verifies falsifies tamper detection.


- **Closure checkpoint:** **ZOCEL-001 checkpoint** — Closed when the ZOCEL-001 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


- **Next action:** **Next action for ZOCEL-001** — Run the acceptance commands of ZOCEL-001 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.



## Breakdown tally

- Requirements: 6
- Falsifiers: 3
- Closure checkpoints: 1
- Next actions: 1

## Court spine

Required courts:

- **ZOCEL-001 exact-subject court** — Independent court for Land the OCEL consumer branch with a real observed run: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


Required evidence:

- **Local execution evidence** — Observed repository-local exact-subject execution evidence.

- **Process evidence** — Observed process evidence kept separate from normative specification.

- **Receipt evidence** — Durable receipt evidence for the exact subject and transition.

- **Replay evidence** — Fresh replay over pack, dependency, graph, consequence, toolchain, and environment identities.

- **Runtime evidence** — Runtime execution evidence for the exact admitted subject.

- **Source evidence** — Exact canonical-source identity evidence.


Required receipt classes:


- replay

- verification



Authority requirement:


- NONE



## Promotion rule

Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

## Path scope


- ontology

- scripts

- src

- test


