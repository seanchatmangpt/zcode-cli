
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-006. DO NOT EDIT. -->
# ARD: ZOCEL-006 — Consumer residue hardening

Close the residual consumer risks: decide the zod dependency so the zod schemas typecheck, record a contract fixture from a real runtime turn, review the loop ontology, and define how a fresh clone finds the pack root.

## Decision boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** 5e20ab3c9dff97edd35deee6759c5e5961ee1a05
- **Exact subject:** zcode-cli:consumer-residue-hardening
- **Standing:** UNKNOWN
- **Evidence ceiling:** IMPLEMENTED_UNVERIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.21:ZOCEL-006
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-006

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Decision D-1: the boundary

**Decision:** `zcode-cli:consumer-residue-hardening` is decided on repository `seanchatmangpt/zcode-cli` at exact
base SHA `5e20ab3c9dff97edd35deee6759c5e5961ee1a05`; recorded standing `UNKNOWN`.

**Consequences:** the consequences of this decision are confined to its declared
path scope:


- ontology/zcode-loop.ttl

- schemas.zod.ts



**Verification:** replay identity `semantic-jira:v26.9.21:ZOCEL-006` re-renders this boundary
byte-identically from the same canonical graph.

## Decision D-2: authority

**Decision:** authority ceiling `CONSTRUCT`; authority requirement:


- NONE



**Consequences:** an authority ceiling outside OBSERVE/SELECT/CONSTRUCT refuses
admission; this record documents the decision and carries no authority of its own.

**Verification:** evidence ceiling `IMPLEMENTED_UNVERIFIED`; admission is receipted
in the required receipt classes:


- verification



## Decision D-3: promotion

**Decision:** Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

**Consequences:** promotion is decided only by the required courts:

- **ZOCEL-006 exact-subject court** — Independent court for Consumer residue hardening: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


**Verification:** the courts accept only the required evidence:

- **Local execution evidence** — Observed repository-local exact-subject execution evidence.

- **Receipt evidence** — Durable receipt evidence for the exact subject and transition.

- **Runtime evidence** — Runtime execution evidence for the exact admitted subject.

- **Source evidence** — Exact canonical-source identity evidence.


## Decision D-4: falsifiability

**Decision:** standing `UNKNOWN` holds only while every falsifier of this
decision stays unobserved.

**Consequences:** each falsifier below is an executable refutation condition in the
canonical graph; observing one falsifies this decision.

- **Hand-written fixture** — A fixture not captured from a real runtime turn falsifies the contract claim.

- **Insensitive fixture** — A shape change that leaves the fixture test green falsifies fixture sensitivity.


**Verification:** each acceptance criterion below is a node in the canonical graph
that must hold for this decision to stand.

- **Zod decision typechecks** — cd /Users/sac/dev/zcode-cli && bun run typecheck; echo rc=$?. Expected: rc=0 with the zod decision recorded (zod in package.json dependencies or schemas.zod.ts removed). Earns: TYPECHECK_OK.

- **Real-turn contract fixture** — cd /Users/sac/dev/zcode-cli && test -s test/fixtures/runtime-turn.contract.json && bun test test/ocel-launcher.test.ts exits 0; then in a copy of the fixture rename one event field with sed -i.bak 's/"type"/"kind"/' and rerun the test against the copy: exit nonzero. Both required. Earns: CONTRACT_SENSITIVE.

- **Ontology reviewed** — cd /Users/sac/dev/zcode-cli && python3 -c "import rdflib;g=rdflib.Graph();g.parse('ontology/zcode-loop.ttl');print(len(g))"; echo rc=$?. Expected: rc=0 and a triple count > 0, plus a review-outcome line in the ZOCEL-006 receipt. Earns: ONTOLOGY_PARSES.

- **Fresh-clone pack root** — mkdir -p /Users/sac/wt/zocel-runs && rm -rf /Users/sac/wt/zocel-runs/fresh && git clone --branch $(git -C /Users/sac/wt/zcode-ocel-consumer branch --show-current) /Users/sac/wt/zcode-ocel-consumer /Users/sac/wt/zocel-runs/fresh && cd /Users/sac/wt/zocel-runs/fresh && bun install && bun scripts/gen-ocel.ts --check; echo rc=$?. Expected: rc=0 with no STALE line. Earns: PACK_ROOT_PORTABLE.

