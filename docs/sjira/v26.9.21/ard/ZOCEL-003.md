
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-003. DO NOT EDIT. -->
# ARD: ZOCEL-003 — Strict toolchain gates that fail instead of skip

Make OCEL coverage and reuse tests fail rather than skip when required toolchains or bundles are absent, and wire that strictness into the locked sync and release build paths.

## Decision boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** 5e20ab3c9dff97edd35deee6759c5e5961ee1a05
- **Exact subject:** zcode-cli:strict-toolchain-gates
- **Standing:** UNKNOWN
- **Evidence ceiling:** IMPLEMENTED_UNVERIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.21:ZOCEL-003
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-003

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Decision D-1: the boundary

**Decision:** `zcode-cli:strict-toolchain-gates` is decided on repository `seanchatmangpt/zcode-cli` at exact
base SHA `5e20ab3c9dff97edd35deee6759c5e5961ee1a05`; recorded standing `UNKNOWN`.

**Consequences:** the consequences of this decision are confined to its declared
path scope:


- package.json

- test



**Verification:** replay identity `semantic-jira:v26.9.21:ZOCEL-003` re-renders this boundary
byte-identically from the same canonical graph.

## Decision D-2: authority

**Decision:** authority ceiling `CONSTRUCT`; authority requirement:


- NONE



**Consequences:** an authority ceiling outside OBSERVE/SELECT/CONSTRUCT refuses
admission; this record documents the decision and carries no authority of its own.

**Verification:** evidence ceiling `IMPLEMENTED_UNVERIFIED`; admission is receipted
in the required receipt classes:


- verification



## Decision D-3: promotion

**Decision:** Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

**Consequences:** promotion is decided only by the required courts:

- **ZOCEL-003 exact-subject court** — Independent court for Strict toolchain gates that fail instead of skip: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


**Verification:** the courts accept only the required evidence:

- **Local execution evidence** — Observed repository-local exact-subject execution evidence.

- **Receipt evidence** — Durable receipt evidence for the exact subject and transition.

- **Source evidence** — Exact canonical-source identity evidence.


## Decision D-4: falsifiability

**Decision:** standing `UNKNOWN` holds only while every falsifier of this
decision stays unobserved.

**Consequences:** each falsifier below is an executable refutation condition in the
canonical graph; observing one falsifies this decision.

- **Silent skip** — A run with a missing toolchain that exits 0 with skipped tests falsifies the strict gate.

- **Unwired gate** — Either variable absent from sync:locked or release:build falsifies wiring.


**Verification:** each acceptance criterion below is a node in the canonical graph
that must hold for this decision to stand.

- **Toolchain strictness fails** — cd /Users/sac/dev/zcode-cli && env -i PATH=/usr/bin:/bin HOME=$HOME ZCODE_REQUIRE_TOOLCHAINS=1 /Users/sac/.bun/bin/bun test test/ocel-coverage.test.ts test/ocel-reuse.test.ts; echo rc=$?. Expected: rc nonzero and no 'skip' line counted as pass. rc=0 with skipped => FAIL. Earns: STRICT_TOOLCHAIN_GATE.

- **Bundle strictness fails** — cd /Users/sac/dev/zcode-cli && ZCODE_REQUIRE_BUNDLE=1 bun test test/ocel-reuse.test.ts with the bundle output directory absent; echo rc=$?. Expected: rc nonzero, not skipped. Earns: STRICT_BUNDLE_GATE.

- **Wired into release paths** — cd /Users/sac/dev/zcode-cli && grep -c ZCODE_REQUIRE_TOOLCHAINS package.json scripts/build-release.ts scripts/sync-runtime.ts and grep -c ZCODE_REQUIRE_BUNDLE on the same files: sum of each variable across the files reachable from sync:locked and release:build is >= 1, exit 0 from grep -q for both variables per script chain. Earns: GATE_WIRED.

