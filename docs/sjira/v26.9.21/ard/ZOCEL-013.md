
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-013. DO NOT EDIT. -->
# ARD: ZOCEL-013 — Quarantine or fix the pre-existing failing tests so release:build can pass

Main has about 10 pre-existing failing tests (subagent-builtin-provider override, launcher keyless prompt, execution-state-tui x2, prompt-preflight x3, model-catalog-tui registry reload, TUI notifications x2), so test:all and therefore release:build exit 1. Fix them, or record a human-approved baseline-exclusion list.

## Decision boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** 5e20ab3c9dff97edd35deee6759c5e5961ee1a05
- **Exact subject:** zcode-cli:main-baseline-failures
- **Standing:** UNKNOWN
- **Evidence ceiling:** SPECIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.21:ZOCEL-013
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-013

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Decision D-1: the boundary

**Decision:** `zcode-cli:main-baseline-failures` is decided on repository `seanchatmangpt/zcode-cli` at exact
base SHA `5e20ab3c9dff97edd35deee6759c5e5961ee1a05`; recorded standing `UNKNOWN`.

**Consequences:** the consequences of this decision are confined to its declared
path scope:


- scripts

- src

- test



**Verification:** replay identity `semantic-jira:v26.9.21:ZOCEL-013` re-renders this boundary
byte-identically from the same canonical graph.

## Decision D-2: authority

**Decision:** authority ceiling `CONSTRUCT`; authority requirement:


- NONE



**Consequences:** an authority ceiling outside OBSERVE/SELECT/CONSTRUCT refuses
admission; this record documents the decision and carries no authority of its own.

**Verification:** evidence ceiling `SPECIFIED`; admission is receipted
in the required receipt classes:


- verification



## Decision D-3: promotion

**Decision:** Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

**Consequences:** promotion is decided only by the required courts:

- **ZOCEL-013 exact-subject court** — Independent court for Quarantine or fix the pre-existing failing tests so release:build can pass: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


**Verification:** the courts accept only the required evidence:

- **Local execution evidence** — Observed repository-local exact-subject execution evidence.

- **Receipt evidence** — Durable receipt evidence for the exact subject and transition.

- **Source evidence** — Exact canonical-source identity evidence.


## Decision D-4: falsifiability

**Decision:** standing `UNKNOWN` holds only while every falsifier of this
decision stays unobserved.

**Consequences:** each falsifier below is an executable refutation condition in the
canonical graph; observing one falsifies this decision.

- **Failure absent from baseline** — Any failing test in the run that is absent from the baseline list falsifies the subset claim.

- **Unapproved exclusion** — A baseline-exclusion list without a human approved-by line falsifies the baseline route.


**Verification:** each acceptance criterion below is a node in the canonical graph
that must hold for this decision to stand.

- **Release build passes or baseline approved** — cd /Users/sac/dev/zcode-cli && bun run release:build; echo rc=$?. Expected: rc=0, OR a recorded human-approved baseline-exclusion list at /Users/sac/wt/zocel-runs/main-fail-baseline.txt whose last line starts approved-by: naming the human (grep -c '^approved-by:' prints 1). Neither => FAIL. Earns: RELEASE_BUILD_PASS or BASELINE_APPROVED.

- **Failures subset of baseline** — mkdir -p /Users/sac/wt/zocel-runs && cd /Users/sac/dev/zcode-cli && bun test 2>&1 | grep -E '^\(fail\)' | sort -u > /Users/sac/wt/zocel-runs/now-fail.txt; comm -23 /Users/sac/wt/zocel-runs/now-fail.txt /Users/sac/wt/zocel-runs/main-fail-baseline.txt. Expected: prints nothing (every failure is listed in the baseline). Earns: SUBSET_OF_BASELINE.

