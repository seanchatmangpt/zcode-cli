
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-005. DO NOT EDIT. -->
# ARD: ZOCEL-005 — Coverage gate, conformance replay, and importer cross-validation

Add an OCEL coverage gate and conformance replay, and cross-validate a real run's OCEL log by importing it with an independent process-mining importer.

## Decision boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** 5e20ab3c9dff97edd35deee6759c5e5961ee1a05
- **Exact subject:** zcode-cli:ocel-coverage-conformance-import
- **Standing:** UNKNOWN
- **Evidence ceiling:** IMPLEMENTED_UNVERIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.21:ZOCEL-005
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-005

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Decision D-1: the boundary

**Decision:** `zcode-cli:ocel-coverage-conformance-import` is decided on repository `seanchatmangpt/zcode-cli` at exact
base SHA `5e20ab3c9dff97edd35deee6759c5e5961ee1a05`; recorded standing `UNKNOWN`.

**Consequences:** the consequences of this decision are confined to its declared
path scope:


- scripts

- src

- test



**Verification:** replay identity `semantic-jira:v26.9.21:ZOCEL-005` re-renders this boundary
byte-identically from the same canonical graph.

## Decision D-2: authority

**Decision:** authority ceiling `CONSTRUCT`; authority requirement:


- NONE



**Consequences:** an authority ceiling outside OBSERVE/SELECT/CONSTRUCT refuses
admission; this record documents the decision and carries no authority of its own.

**Verification:** evidence ceiling `IMPLEMENTED_UNVERIFIED`; admission is receipted
in the required receipt classes:


- replay

- verification



## Decision D-3: promotion

**Decision:** Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

**Consequences:** promotion is decided only by the required courts:

- **ZOCEL-005 exact-subject court** — Independent court for Coverage gate, conformance replay, and importer cross-validation: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


**Verification:** the courts accept only the required evidence:

- **Local execution evidence** — Observed repository-local exact-subject execution evidence.

- **Process evidence** — Observed process evidence kept separate from normative specification.

- **Replay evidence** — Fresh replay over pack, dependency, graph, consequence, toolchain, and environment identities.

- **Runtime evidence** — Runtime execution evidence for the exact admitted subject.

- **Source evidence** — Exact canonical-source identity evidence.


## Decision D-4: falsifiability

**Decision:** standing `UNKNOWN` holds only while every falsifier of this
decision stays unobserved.

**Consequences:** each falsifier below is an executable refutation condition in the
canonical graph; observing one falsifies this decision.

- **Synthetic log only** — An importer run over a hand-written log rather than a real run falsifies cross-validation.

- **Count mismatch** — Event or object counts that differ between source and importer falsify the import claim.


**Verification:** each acceptance criterion below is a node in the canonical graph
that must hold for this decision to stand.

- **Coverage gate enforced** — mkdir -p /Users/sac/wt/zocel-runs && cd /Users/sac/dev/zcode-cli && bun test test/ocel-coverage.test.ts exits 0 over the real log "$LOG"; then delete one declared event type's lines from a copy /Users/sac/wt/zocel-runs/cov-copy.jsonocel and rerun against the copy: exit nonzero. Both outcomes required. Earns: COVERAGE_ENFORCED.

- **Conformance replay passes** — cd /Users/sac/dev/zcode-cli && bun test test/ocel-conformance.test.ts exits 0 and replay of "$LOG" prints 'deviations: 0' (zero deviations). No real LOG => NOT_RUN. Earns: REPLAY_CONFORMANT.

- **Importer accepts the log** — LOG=$(ls -t /Users/sac/wt/zocel-runs/*/*.jsonocel | head -1); import "$LOG" with the ex4pm importer CLI (ls /Users/sac/ex4pm exists; /Users/sac/wasm4pm is absent, use it only if it appears); expected exit 0 and printed event and object counts equal to jq '.events|length' and jq '.objects|length' of "$LOG". No LOG or no importer => BLOCKED, not ALIVE. Earns: IMPORT_CROSS_VALIDATED.

