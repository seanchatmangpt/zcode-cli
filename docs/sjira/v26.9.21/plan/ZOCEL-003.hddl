
;; GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-003. DO NOT EDIT.
;;
;; Zero-authority projection: this HDDL file is the promotion plan AS DECLARED
;; by the canonical RDF graph. It executes nothing and holds no authority to
;; actuate, merge, publish, deploy, or promote standing. It is a
;; plan-as-declared projection, not a solved or executed plan: the subtask
;; order below is the declared court / evidence / receipt-class / falsifier /
;; checkpoint structure of the graph in stable-key order and carries no
;; solver-derived ordering. The declared goal follows
;; shapes/work-order.shacl.ttl: standing ALIVE only with the receipt crown.
;; The real reconciliation receipt binds this file to the exact ontology bytes
;; through receipt metadata graph_hash.

(defproblem promote-ZOCEL-003 semantic-jira-workorder
  (:objects
    ZOCEL-003 - workorder
    court-zocel-003 - court
    local-execution-evidence receipt-evidence source-evidence - evidence
    fal-zocel-003-1 fal-zocel-003-2 - falsifier
    cp-zocel-003 - checkpoint
    edge-zocel-003-on-001 - dependency-edge)
  (:task (promote ZOCEL-003 ALIVE))
  (:method decompose-promote-ZOCEL-003
    :task (promote ZOCEL-003 ALIVE)
    :subtasks (
      ;; required courts - sj:requiresCourt - stable-key order
      (close-court ZOCEL-003 court-zocel-003) ;; ZOCEL-003 exact-subject court
      ;; required evidence - sj:requiresEvidence - stable-key order
      (produce-evidence ZOCEL-003 local-execution-evidence) ;; Local execution evidence
      (produce-evidence ZOCEL-003 receipt-evidence) ;; Receipt evidence
      (produce-evidence ZOCEL-003 source-evidence) ;; Source evidence
      ;; required receipt classes - sj:requiresReceiptClass - stable-key order
      (record-receipt-class ZOCEL-003 verification)
      ;; falsifiers to discharge - sj:falsifier - stable-key order
      (discharge-falsifier ZOCEL-003 fal-zocel-003-1) ;; Silent skip
      (discharge-falsifier ZOCEL-003 fal-zocel-003-2) ;; Unwired gate
      ;; terminal checkpoint - sj:nextCheckpoint
      (reach-checkpoint ZOCEL-003 cp-zocel-003) ;; ZOCEL-003 checkpoint
      ))
  (:init
    (workorder ZOCEL-003)
    (standing ZOCEL-003 UNKNOWN)
    (base-sha ZOCEL-003 5e20ab3c9dff97edd35deee6759c5e5961ee1a05)
    (authority-ceiling ZOCEL-003 CONSTRUCT)
    (exact-subject ZOCEL-003 "zcode-cli:strict-toolchain-gates")
    (repository ZOCEL-003 "seanchatmangpt/zcode-cli")
    (replay-identity ZOCEL-003 "semantic-jira:v26.9.21:ZOCEL-003")
    (projection-authority NONE)
    (depends-on ZOCEL-003 edge-zocel-003-on-001))
  ;; Declared goal, not observed standing: ALIVE with the receipt crown.
  (:goal (and (standing ZOCEL-003 ALIVE) (has-receipt ZOCEL-003))))
