
;; GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-002. DO NOT EDIT.
;;
;; Zero-authority projection: this HDDL file is the promotion plan AS DECLARED
;; by the canonical RDF graph. It executes nothing and holds no authority to
;; actuate, merge, publish, deploy, or promote standing. It is a
;; plan-as-declared projection, not a solved or executed plan: the subtask
;; order below is the declared court / evidence / receipt-class / falsifier /
;; checkpoint structure of the graph in stable-key order and carries no
;; solver-derived ordering. The declared goal follows
;; shapes/work-order.shacl.ttl: standing ALIVE only with the receipt crown.
;; The real reconciliation receipt binds this file to the exact ontology bytes
;; through receipt metadata graph_hash.

(defproblem promote-ZOCEL-002 semantic-jira-workorder
  (:objects
    ZOCEL-002 - workorder
    court-zocel-002 - court
    local-execution-evidence receipt-evidence source-evidence - evidence
    fal-zocel-002-1 fal-zocel-002-2 - falsifier
    cp-zocel-002 - checkpoint
    edge-zocel-002-on-001 - dependency-edge)
  (:task (promote ZOCEL-002 ALIVE))
  (:method decompose-promote-ZOCEL-002
    :task (promote ZOCEL-002 ALIVE)
    :subtasks (
      ;; required courts - sj:requiresCourt - stable-key order
      (close-court ZOCEL-002 court-zocel-002) ;; ZOCEL-002 exact-subject court
      ;; required evidence - sj:requiresEvidence - stable-key order
      (produce-evidence ZOCEL-002 local-execution-evidence) ;; Local execution evidence
      (produce-evidence ZOCEL-002 receipt-evidence) ;; Receipt evidence
      (produce-evidence ZOCEL-002 source-evidence) ;; Source evidence
      ;; required receipt classes - sj:requiresReceiptClass - stable-key order
      (record-receipt-class ZOCEL-002 verification)
      ;; falsifiers to discharge - sj:falsifier - stable-key order
      (discharge-falsifier ZOCEL-002 fal-zocel-002-1) ;; Divergent digest
      (discharge-falsifier ZOCEL-002 fal-zocel-002-2) ;; Private vectors
      ;; terminal checkpoint - sj:nextCheckpoint
      (reach-checkpoint ZOCEL-002 cp-zocel-002) ;; ZOCEL-002 checkpoint
      ))
  (:init
    (workorder ZOCEL-002)
    (standing ZOCEL-002 UNKNOWN)
    (base-sha ZOCEL-002 1aceb47e935200c6f67960fa2b6125d1a24fae85)
    (authority-ceiling ZOCEL-002 CONSTRUCT)
    (exact-subject ZOCEL-002 "zcode-cli:ocel-ts-py-parity")
    (repository ZOCEL-002 "seanchatmangpt/zcode-cli")
    (replay-identity ZOCEL-002 "semantic-jira:v26.9.21:ZOCEL-002")
    (projection-authority NONE)
    (depends-on ZOCEL-002 edge-zocel-002-on-001))
  ;; Declared goal, not observed standing: ALIVE with the receipt crown.
  (:goal (and (standing ZOCEL-002 ALIVE) (has-receipt ZOCEL-002))))
