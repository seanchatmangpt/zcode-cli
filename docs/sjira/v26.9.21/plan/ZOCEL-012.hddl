
;; GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-012. DO NOT EDIT.
;;
;; Zero-authority projection: this HDDL file is the promotion plan AS DECLARED
;; by the canonical RDF graph. It executes nothing and holds no authority to
;; actuate, merge, publish, deploy, or promote standing. It is a
;; plan-as-declared projection, not a solved or executed plan: the subtask
;; order below is the declared court / evidence / receipt-class / falsifier /
;; checkpoint structure of the graph in stable-key order and carries no
;; solver-derived ordering. The declared goal follows
;; shapes/work-order.shacl.ttl: standing ALIVE only with the receipt crown.
;; The real reconciliation receipt binds this file to the exact ontology bytes
;; through receipt metadata graph_hash.

(defproblem promote-ZOCEL-012 semantic-jira-workorder
  (:objects
    ZOCEL-012 - workorder
    court-zocel-012 - court
    local-execution-evidence receipt-evidence - evidence
    fal-zocel-012-1 fal-zocel-012-2 - falsifier
    cp-zocel-012 - checkpoint)
  (:task (promote ZOCEL-012 ALIVE))
  (:method decompose-promote-ZOCEL-012
    :task (promote ZOCEL-012 ALIVE)
    :subtasks (
      ;; required courts - sj:requiresCourt - stable-key order
      (close-court ZOCEL-012 court-zocel-012) ;; ZOCEL-012 exact-subject court
      ;; required evidence - sj:requiresEvidence - stable-key order
      (produce-evidence ZOCEL-012 local-execution-evidence) ;; Local execution evidence
      (produce-evidence ZOCEL-012 receipt-evidence) ;; Receipt evidence
      ;; required receipt classes - sj:requiresReceiptClass - stable-key order
      (record-receipt-class ZOCEL-012 verification)
      ;; falsifiers to discharge - sj:falsifier - stable-key order
      (discharge-falsifier ZOCEL-012 fal-zocel-012-1) ;; Unlisted worktree
      (discharge-falsifier ZOCEL-012 fal-zocel-012-2) ;; Agent deletion
      ;; terminal checkpoint - sj:nextCheckpoint
      (reach-checkpoint ZOCEL-012 cp-zocel-012) ;; ZOCEL-012 checkpoint
      ))
  (:init
    (workorder ZOCEL-012)
    (standing ZOCEL-012 UNKNOWN)
    (base-sha ZOCEL-012 1aceb47e935200c6f67960fa2b6125d1a24fae85)
    (authority-ceiling ZOCEL-012 OBSERVE)
    (exact-subject ZOCEL-012 "zcode-cli:worktree-hygiene")
    (repository ZOCEL-012 "seanchatmangpt/zcode-cli")
    (replay-identity ZOCEL-012 "semantic-jira:v26.9.21:ZOCEL-012")
    (projection-authority NONE))
  ;; Declared goal, not observed standing: ALIVE with the receipt crown.
  (:goal (and (standing ZOCEL-012 ALIVE) (has-receipt ZOCEL-012))))
