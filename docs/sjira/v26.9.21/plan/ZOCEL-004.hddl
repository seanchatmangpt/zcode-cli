
;; GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-004. DO NOT EDIT.
;;
;; Zero-authority projection: this HDDL file is the promotion plan AS DECLARED
;; by the canonical RDF graph. It executes nothing and holds no authority to
;; actuate, merge, publish, deploy, or promote standing. It is a
;; plan-as-declared projection, not a solved or executed plan: the subtask
;; order below is the declared court / evidence / receipt-class / falsifier /
;; checkpoint structure of the graph in stable-key order and carries no
;; solver-derived ordering. The declared goal follows
;; shapes/work-order.shacl.ttl: standing ALIVE only with the receipt crown.
;; The real reconciliation receipt binds this file to the exact ontology bytes
;; through receipt metadata graph_hash.

(defproblem promote-ZOCEL-004 semantic-jira-workorder
  (:objects
    ZOCEL-004 - workorder
    court-zocel-004 - court
    local-execution-evidence receipt-evidence source-evidence - evidence
    fal-zocel-004-1 fal-zocel-004-2 - falsifier
    cp-zocel-004 - checkpoint)
  (:task (promote ZOCEL-004 ALIVE))
  (:method decompose-promote-ZOCEL-004
    :task (promote ZOCEL-004 ALIVE)
    :subtasks (
      ;; required courts - sj:requiresCourt - stable-key order
      (close-court ZOCEL-004 court-zocel-004) ;; ZOCEL-004 exact-subject court
      ;; required evidence - sj:requiresEvidence - stable-key order
      (produce-evidence ZOCEL-004 local-execution-evidence) ;; Local execution evidence
      (produce-evidence ZOCEL-004 receipt-evidence) ;; Receipt evidence
      (produce-evidence ZOCEL-004 source-evidence) ;; Source evidence
      ;; required receipt classes - sj:requiresReceiptClass - stable-key order
      (record-receipt-class ZOCEL-004 verification)
      ;; falsifiers to discharge - sj:falsifier - stable-key order
      (discharge-falsifier ZOCEL-004 fal-zocel-004-1) ;; Baseline drift
      (discharge-falsifier ZOCEL-004 fal-zocel-004-2) ;; Pack count drift
      ;; terminal checkpoint - sj:nextCheckpoint
      (reach-checkpoint ZOCEL-004 cp-zocel-004) ;; ZOCEL-004 checkpoint
      ))
  (:init
    (workorder ZOCEL-004)
    (standing ZOCEL-004 UNKNOWN)
    (base-sha ZOCEL-004 1aceb47e935200c6f67960fa2b6125d1a24fae85)
    (authority-ceiling ZOCEL-004 CONSTRUCT)
    (exact-subject ZOCEL-004 "ggen-marketplace:merged-packs-qualification")
    (repository ZOCEL-004 "seanchatmangpt/zcode-cli")
    (replay-identity ZOCEL-004 "semantic-jira:v26.9.21:ZOCEL-004")
    (projection-authority NONE))
  ;; Declared goal, not observed standing: ALIVE with the receipt crown.
  (:goal (and (standing ZOCEL-004 ALIVE) (has-receipt ZOCEL-004))))
