
;; GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-001. DO NOT EDIT.
;;
;; Zero-authority projection: this HDDL file is the promotion plan AS DECLARED
;; by the canonical RDF graph. It executes nothing and holds no authority to
;; actuate, merge, publish, deploy, or promote standing. It is a
;; plan-as-declared projection, not a solved or executed plan: the subtask
;; order below is the declared court / evidence / receipt-class / falsifier /
;; checkpoint structure of the graph in stable-key order and carries no
;; solver-derived ordering. The declared goal follows
;; shapes/work-order.shacl.ttl: standing ALIVE only with the receipt crown.
;; The real reconciliation receipt binds this file to the exact ontology bytes
;; through receipt metadata graph_hash.

(defproblem promote-ZOCEL-001 semantic-jira-workorder
  (:objects
    ZOCEL-001 - workorder
    court-zocel-001 - court
    local-execution-evidence process-evidence receipt-evidence replay-evidence runtime-evidence source-evidence - evidence
    fal-zocel-001-1 fal-zocel-001-2 fal-zocel-001-3 - falsifier
    cp-zocel-001 - checkpoint)
  (:task (promote ZOCEL-001 ALIVE))
  (:method decompose-promote-ZOCEL-001
    :task (promote ZOCEL-001 ALIVE)
    :subtasks (
      ;; required courts - sj:requiresCourt - stable-key order
      (close-court ZOCEL-001 court-zocel-001) ;; ZOCEL-001 exact-subject court
      ;; required evidence - sj:requiresEvidence - stable-key order
      (produce-evidence ZOCEL-001 local-execution-evidence) ;; Local execution evidence
      (produce-evidence ZOCEL-001 process-evidence) ;; Process evidence
      (produce-evidence ZOCEL-001 receipt-evidence) ;; Receipt evidence
      (produce-evidence ZOCEL-001 replay-evidence) ;; Replay evidence
      (produce-evidence ZOCEL-001 runtime-evidence) ;; Runtime evidence
      (produce-evidence ZOCEL-001 source-evidence) ;; Source evidence
      ;; required receipt classes - sj:requiresReceiptClass - stable-key order
      (record-receipt-class ZOCEL-001 replay)
      (record-receipt-class ZOCEL-001 verification)
      ;; falsifiers to discharge - sj:falsifier - stable-key order
      (discharge-falsifier ZOCEL-001 fal-zocel-001-1) ;; Fixture-only evidence
      (discharge-falsifier ZOCEL-001 fal-zocel-001-2) ;; Stale generated code
      (discharge-falsifier ZOCEL-001 fal-zocel-001-3) ;; Tamper undetected
      ;; terminal checkpoint - sj:nextCheckpoint
      (reach-checkpoint ZOCEL-001 cp-zocel-001) ;; ZOCEL-001 checkpoint
      ))
  (:init
    (workorder ZOCEL-001)
    (standing ZOCEL-001 UNKNOWN)
    (base-sha ZOCEL-001 1aceb47e935200c6f67960fa2b6125d1a24fae85)
    (authority-ceiling ZOCEL-001 CONSTRUCT)
    (exact-subject ZOCEL-001 "zcode-cli:feat/zcode-ocel-consumer")
    (repository ZOCEL-001 "seanchatmangpt/zcode-cli")
    (replay-identity ZOCEL-001 "semantic-jira:v26.9.21:ZOCEL-001")
    (projection-authority NONE))
  ;; Declared goal, not observed standing: ALIVE with the receipt crown.
  (:goal (and (standing ZOCEL-001 ALIVE) (has-receipt ZOCEL-001))))
