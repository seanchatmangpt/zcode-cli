
;; GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-009. DO NOT EDIT.
;;
;; Zero-authority projection: this HDDL file is the promotion plan AS DECLARED
;; by the canonical RDF graph. It executes nothing and holds no authority to
;; actuate, merge, publish, deploy, or promote standing. It is a
;; plan-as-declared projection, not a solved or executed plan: the subtask
;; order below is the declared court / evidence / receipt-class / falsifier /
;; checkpoint structure of the graph in stable-key order and carries no
;; solver-derived ordering. The declared goal follows
;; shapes/work-order.shacl.ttl: standing ALIVE only with the receipt crown.
;; The real reconciliation receipt binds this file to the exact ontology bytes
;; through receipt metadata graph_hash.

(defproblem promote-ZOCEL-009 semantic-jira-workorder
  (:objects
    ZOCEL-009 - workorder
    court-zocel-009 - court
    local-execution-evidence receipt-evidence source-evidence - evidence
    fal-zocel-009-1 fal-zocel-009-2 fal-zocel-009-3 - falsifier
    cp-zocel-009 - checkpoint)
  (:task (promote ZOCEL-009 ALIVE))
  (:method decompose-promote-ZOCEL-009
    :task (promote ZOCEL-009 ALIVE)
    :subtasks (
      ;; required courts - sj:requiresCourt - stable-key order
      (close-court ZOCEL-009 court-zocel-009) ;; ZOCEL-009 exact-subject court
      ;; required evidence - sj:requiresEvidence - stable-key order
      (produce-evidence ZOCEL-009 local-execution-evidence) ;; Local execution evidence
      (produce-evidence ZOCEL-009 receipt-evidence) ;; Receipt evidence
      (produce-evidence ZOCEL-009 source-evidence) ;; Source evidence
      ;; required receipt classes - sj:requiresReceiptClass - stable-key order
      (record-receipt-class ZOCEL-009 verification)
      ;; falsifiers to discharge - sj:falsifier - stable-key order
      (discharge-falsifier ZOCEL-009 fal-zocel-009-1) ;; Fixture drift
      (discharge-falsifier ZOCEL-009 fal-zocel-009-2) ;; Untried merge
      (discharge-falsifier ZOCEL-009 fal-zocel-009-3) ;; Unreviewed resolutions
      ;; terminal checkpoint - sj:nextCheckpoint
      (reach-checkpoint ZOCEL-009 cp-zocel-009) ;; ZOCEL-009 checkpoint
      ))
  (:init
    (workorder ZOCEL-009)
    (standing ZOCEL-009 UNKNOWN)
    (base-sha ZOCEL-009 5e20ab3c9dff97edd35deee6759c5e5961ee1a05)
    (authority-ceiling ZOCEL-009 CONSTRUCT)
    (exact-subject ZOCEL-009 "zcode-cli:w9-sweep/zcode-pr3")
    (repository ZOCEL-009 "seanchatmangpt/zcode-cli")
    (replay-identity ZOCEL-009 "semantic-jira:v26.9.21:ZOCEL-009")
    (projection-authority NONE))
  ;; Declared goal, not observed standing: ALIVE with the receipt crown.
  (:goal (and (standing ZOCEL-009 ALIVE) (has-receipt ZOCEL-009))))
