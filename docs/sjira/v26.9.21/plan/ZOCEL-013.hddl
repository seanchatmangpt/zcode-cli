
;; GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-013. DO NOT EDIT.
;;
;; Zero-authority projection: this HDDL file is the promotion plan AS DECLARED
;; by the canonical RDF graph. It executes nothing and holds no authority to
;; actuate, merge, publish, deploy, or promote standing. It is a
;; plan-as-declared projection, not a solved or executed plan: the subtask
;; order below is the declared court / evidence / receipt-class / falsifier /
;; checkpoint structure of the graph in stable-key order and carries no
;; solver-derived ordering. The declared goal follows
;; shapes/work-order.shacl.ttl: standing ALIVE only with the receipt crown.
;; The real reconciliation receipt binds this file to the exact ontology bytes
;; through receipt metadata graph_hash.

(defproblem promote-ZOCEL-013 semantic-jira-workorder
  (:objects
    ZOCEL-013 - workorder
    court-zocel-013 - court
    local-execution-evidence receipt-evidence source-evidence - evidence
    fal-zocel-013-1 fal-zocel-013-2 - falsifier
    cp-zocel-013 - checkpoint)
  (:task (promote ZOCEL-013 ALIVE))
  (:method decompose-promote-ZOCEL-013
    :task (promote ZOCEL-013 ALIVE)
    :subtasks (
      ;; required courts - sj:requiresCourt - stable-key order
      (close-court ZOCEL-013 court-zocel-013) ;; ZOCEL-013 exact-subject court
      ;; required evidence - sj:requiresEvidence - stable-key order
      (produce-evidence ZOCEL-013 local-execution-evidence) ;; Local execution evidence
      (produce-evidence ZOCEL-013 receipt-evidence) ;; Receipt evidence
      (produce-evidence ZOCEL-013 source-evidence) ;; Source evidence
      ;; required receipt classes - sj:requiresReceiptClass - stable-key order
      (record-receipt-class ZOCEL-013 verification)
      ;; falsifiers to discharge - sj:falsifier - stable-key order
      (discharge-falsifier ZOCEL-013 fal-zocel-013-1) ;; Failure absent from baseline
      (discharge-falsifier ZOCEL-013 fal-zocel-013-2) ;; Unapproved exclusion
      ;; terminal checkpoint - sj:nextCheckpoint
      (reach-checkpoint ZOCEL-013 cp-zocel-013) ;; ZOCEL-013 checkpoint
      ))
  (:init
    (workorder ZOCEL-013)
    (standing ZOCEL-013 UNKNOWN)
    (base-sha ZOCEL-013 5e20ab3c9dff97edd35deee6759c5e5961ee1a05)
    (authority-ceiling ZOCEL-013 CONSTRUCT)
    (exact-subject ZOCEL-013 "zcode-cli:main-baseline-failures")
    (repository ZOCEL-013 "seanchatmangpt/zcode-cli")
    (replay-identity ZOCEL-013 "semantic-jira:v26.9.21:ZOCEL-013")
    (projection-authority NONE))
  ;; Declared goal, not observed standing: ALIVE with the receipt crown.
  (:goal (and (standing ZOCEL-013 ALIVE) (has-receipt ZOCEL-013))))
