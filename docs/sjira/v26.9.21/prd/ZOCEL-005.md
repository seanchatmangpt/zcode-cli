
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-005. DO NOT EDIT. -->
# PRD: ZOCEL-005 — Coverage gate, conformance replay, and importer cross-validation

Add an OCEL coverage gate and conformance replay, and cross-validate a real run's OCEL log by importing it with an independent process-mining importer.

## Product boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** 5e20ab3c9dff97edd35deee6759c5e5961ee1a05
- **Exact subject:** zcode-cli:ocel-coverage-conformance-import
- **Standing:** UNKNOWN
- **Evidence ceiling:** IMPLEMENTED_UNVERIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.21:ZOCEL-005
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-005

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Dependencies


- **ZOCEL-005 depends on ZOCEL-001** — ZOCEL-005 requires ZOCEL-001 at standing ALIVE before it may start; standing is not inherited.



## Court spine

Required courts:

- **ZOCEL-005 exact-subject court** — Independent court for Coverage gate, conformance replay, and importer cross-validation: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


Required evidence:

- **Local execution evidence** — Observed repository-local exact-subject execution evidence.

- **Process evidence** — Observed process evidence kept separate from normative specification.

- **Replay evidence** — Fresh replay over pack, dependency, graph, consequence, toolchain, and environment identities.

- **Runtime evidence** — Runtime execution evidence for the exact admitted subject.

- **Source evidence** — Exact canonical-source identity evidence.


Required receipt classes:


- replay

- verification



Authority requirement:


- NONE



## Requirements

Each requirement below is an acceptance criterion node in the canonical graph.

- **Coverage gate enforced** — Run: mkdir -p /Users/sac/wt/zocel-runs && LOG=$(ls -t /Users/sac/wt/zocel-runs/001/*.jsonocel | head -1) && cd /Users/sac/dev/zcode-cli && bun test test/ocel-coverage.test.ts exits 0 over the real log "$LOG"; then copy it with cp "$LOG" /Users/sac/wt/zocel-runs/cov-copy.jsonocel, delete one declared event type's lines from the copy, and rerun against the copy: exit nonzero. Both outcomes required. Earns: COVERAGE_ENFORCED.

- **Conformance replay passes** — Run: mkdir -p /Users/sac/wt/zocel-runs && LOG=$(ls -t /Users/sac/wt/zocel-runs/001/*.jsonocel | head -1) && cd /Users/sac/dev/zcode-cli && bun test test/ocel-conformance.test.ts exits 0 and replay of "$LOG" prints 'deviations: 0' (zero deviations). No real LOG => NOT_RUN. Earns: REPLAY_CONFORMANT.

- **Importer accepts the log** — Run: mkdir -p /Users/sac/wt/zocel-runs && LOG=$(ls -t /Users/sac/wt/zocel-runs/*/*.jsonocel | head -1) && import "$LOG" with the ex4pm importer CLI (ls /Users/sac/ex4pm exists; /Users/sac/wasm4pm is absent, use it only if it appears); expected exit 0 and printed event and object counts equal to jq '.events|length' and jq '.objects|length' of "$LOG". No LOG or no importer => BLOCKED, not ALIVE. Earns: IMPORT_CROSS_VALIDATED.


## Falsifiers

Each falsifier below is an executable refutation condition in the canonical graph.

- **Synthetic log only** — An importer run over a hand-written log rather than a real run falsifies cross-validation.

- **Count mismatch** — Event or object counts that differ between source and importer falsify the import claim.


## Promotion rule

Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

## Path scope


- scripts

- src

- test



## Next checkpoint

- **ZOCEL-005 checkpoint** — Closed when the ZOCEL-005 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


## Next actions

- **Next action for ZOCEL-005** — Run the acceptance commands of ZOCEL-005 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.


## Projection surface

- **Architecture requirements document** — Deterministic architecture requirements document projection; authority is NONE.

- **Executive status view** — Deterministic executive status view projection; authority is NONE.

- **FOND planning projection** — Deterministic fond planning projection projection; authority is NONE.

- **HDDL task/method projection** — Deterministic hddl task/method projection projection; authority is NONE.

- **Jira/Markdown ticket** — Deterministic jira/markdown ticket projection; authority is NONE.

- **Machine status view** — Deterministic machine status view projection; authority is NONE.

- **Product requirements document** — Deterministic product requirements document projection; authority is NONE.

- **Receipt requirement summary** — Deterministic receipt requirement summary projection; authority is NONE.

- **Replay manifest** — Deterministic replay manifest projection; authority is NONE.

- **SA2A work/execution package** — Deterministic sa2a work/execution package projection; authority is NONE.

- **Verification plan** — Deterministic verification plan projection; authority is NONE.

- **Vision document** — Deterministic vision document projection; authority is NONE.

- **Working-backwards plan** — Deterministic working-backwards plan projection; authority is NONE.

- **Bounded worker input envelope** — Deterministic bounded worker input envelope projection; authority is NONE.


## Receipts

- None recorded in the canonical graph.

