
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-001. DO NOT EDIT. -->
# PRD: ZOCEL-001 — Land the OCEL consumer branch with a real observed run

Verification remainder for feat/zcode-ocel-consumer, merged into local main (5e20ab3, not pushed). Observed once: ZCODE_OCEL=1 with --output-format stream-json emitted a 13-event .jsonocel and .receipt.json with an intact verifyChain. Standing stays UNKNOWN until a court receipt exists. Remaining: repeatable exact-head court run, and the app-server source, which was not exercised. Without --output-format stream-json the tap emits nothing.

## Product boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** 5e20ab3c9dff97edd35deee6759c5e5961ee1a05
- **Exact subject:** zcode-cli:feat/zcode-ocel-consumer
- **Standing:** UNKNOWN
- **Evidence ceiling:** IMPLEMENTED_UNVERIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.21:ZOCEL-001
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-001

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Dependencies

- None.


## Court spine

Required courts:

- **ZOCEL-001 exact-subject court** — Independent court for Land the OCEL consumer branch with a real observed run: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


Required evidence:

- **Local execution evidence** — Observed repository-local exact-subject execution evidence.

- **Process evidence** — Observed process evidence kept separate from normative specification.

- **Receipt evidence** — Durable receipt evidence for the exact subject and transition.

- **Replay evidence** — Fresh replay over pack, dependency, graph, consequence, toolchain, and environment identities.

- **Runtime evidence** — Runtime execution evidence for the exact admitted subject.

- **Source evidence** — Exact canonical-source identity evidence.


Required receipt classes:


- replay

- verification



Authority requirement:


- NONE



## Requirements

Each requirement below is an acceptance criterion node in the canonical graph.

- **Static gates pass** — cd /Users/sac/dev/zcode-cli (main) && git rev-parse HEAD equals the pinned baseSha, then 'bun run typecheck' exits 0, 'bun test test/ocel-*.test.ts' reports 0 fail, and 'bun scripts/gen-ocel.ts --check' prints no STALE line and exits 0.

- **Real run emits log and receipt** — Run: mkdir -p /Users/sac/wt/zocel-runs && touch /Users/sac/wt/zocel-runs/.mark && cd /Users/sac/dev/zcode-cli && ZCODE_OCEL=1 ZCODE_OCEL_DIR=/Users/sac/wt/zocel-runs/001 node bin/zcode.js -p 'Reply with exactly: PONG' --output-format stream-json. Expected: exit 0 and one *.jsonocel plus a sibling *.receipt.json found by: LOG=$(find /Users/sac/wt/zocel-runs/001 -name '*.jsonocel' -newer /Users/sac/wt/zocel-runs/.mark | head -1); test -s "$LOG" && test -s "${LOG%.jsonocel}.receipt.json" exits 0. No log or no receipt => standing NOT_RUN, never ALIVE. Earns: OBSERVED (real-run evidence) for the stream-json emit claim.

- **Log validates against OCEL 2.0 schema** — After acc-zocel-001-2 (LOG set as there): python3 -c "import json,jsonschema,sys; jsonschema.validate(json.load(open(sys.argv[1])), json.load(open('/Users/sac/gymact/src/gymact/schemas/ocel20-schema.json')))" "$LOG" exits 0 and prints nothing (zero validation errors). No LOG => NOT_RUN. Earns: SCHEMA_VALID for the log; ALIVE only with acc-zocel-001-4..6.

- **Receipt chain verifies** — cd /Users/sac/dev/zcode-cli && bun scripts/gen-ocel.ts --check exits 0, then the generated verifier under src/ (path printed by gen-ocel.ts) run over "$LOG" and "${LOG%.jsonocel}.receipt.json" exits 0 and prints a chain-verified line. Nonzero exit or no LOG => NOT_RUN/FAIL. Earns: CHAIN_VERIFIED.

- **App-server tap capture** — App-server source capture, not yet exercised: mkdir -p /Users/sac/wt/zocel-runs/001-app && cd /Users/sac/dev/zcode-cli && drive 'ZCODE_OCEL=1 ZCODE_OCEL_DIR=/Users/sac/wt/zocel-runs/001-app node bin/zcode.js app-server' over stdio with one real prompt turn, then find /Users/sac/wt/zocel-runs/001-app -name '*.jsonocel' lists one file with a sibling *.receipt.json that verifyChain accepts. No file => NOT_RUN. Earns: APP_SERVER_TAP_OBSERVED.

- **Seal-twice refused and tamper detected** — mkdir -p /Users/sac/wt/zocel-runs && cd /Users/sac/dev/zcode-cli && bun test test/ocel-generated.test.ts exits 0 with 0 fail, covering: a second seal of the same session returns a typed error (not exit 0 silently), and copying "$LOG" to /Users/sac/wt/zocel-runs/tamper.jsonocel, flipping one byte with printf '\\x00' | dd of=/Users/sac/wt/zocel-runs/tamper.jsonocel bs=1 seek=10 conv=notrunc, makes the verifier exit nonzero. Earns: TAMPER_EVIDENT.


## Falsifiers

Each falsifier below is an executable refutation condition in the canonical graph.

- **Fixture-only evidence** — Observation: a green bun test with 'find ~/.zcode /Users/sac/wt/zocel-runs -name *.jsonocel -newer /Users/sac/wt/zocel-runs/.mark' returning no file refutes ALIVE; standing stays NOT_RUN.

- **Stale generated code** — Observation: 'bun scripts/gen-ocel.ts --check' in /Users/sac/wt/zcode-ocel-consumer printing a line containing STALE refutes the claim that the generated verifier and table match ontology/zcode-loop.ttl.

- **Tamper undetected** — Observation: the verifier exiting 0 on a copy of the log with one byte flipped (dd conv=notrunc) refutes tamper detection.


## Promotion rule

Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

## Path scope


- ontology

- scripts

- src

- test



## Next checkpoint

- **ZOCEL-001 checkpoint** — Closed when the ZOCEL-001 court returns a receipt at the exact head; the frontier then advances to dependents of this work order.


## Next actions

- **Next action for ZOCEL-001** — Run the acceptance commands of ZOCEL-001 at an exact head in the scoped worktree, preserve failure output as evidence, and never rerun an unchanged failure without a new hypothesis.


## Projection surface

- **Architecture requirements document** — Deterministic architecture requirements document projection; authority is NONE.

- **Executive status view** — Deterministic executive status view projection; authority is NONE.

- **FOND planning projection** — Deterministic fond planning projection projection; authority is NONE.

- **HDDL task/method projection** — Deterministic hddl task/method projection projection; authority is NONE.

- **Jira/Markdown ticket** — Deterministic jira/markdown ticket projection; authority is NONE.

- **Machine status view** — Deterministic machine status view projection; authority is NONE.

- **Product requirements document** — Deterministic product requirements document projection; authority is NONE.

- **Receipt requirement summary** — Deterministic receipt requirement summary projection; authority is NONE.

- **Replay manifest** — Deterministic replay manifest projection; authority is NONE.

- **SA2A work/execution package** — Deterministic sa2a work/execution package projection; authority is NONE.

- **Verification plan** — Deterministic verification plan projection; authority is NONE.

- **Vision document** — Deterministic vision document projection; authority is NONE.

- **Working-backwards plan** — Deterministic working-backwards plan projection; authority is NONE.

- **Bounded worker input envelope** — Deterministic bounded worker input envelope projection; authority is NONE.


## Receipts

- None recorded in the canonical graph.

