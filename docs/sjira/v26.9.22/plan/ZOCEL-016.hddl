
;; GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-016. DO NOT EDIT.
;;
;; Zero-authority projection: this HDDL file is the promotion plan AS DECLARED
;; by the canonical RDF graph. It executes nothing and holds no authority to
;; actuate, merge, publish, deploy, or promote standing. It is a
;; plan-as-declared projection, not a solved or executed plan: the subtask
;; order below is the declared court / evidence / receipt-class / falsifier /
;; checkpoint structure of the graph in stable-key order and carries no
;; solver-derived ordering. The declared goal follows
;; shapes/work-order.shacl.ttl: standing ALIVE only with the receipt crown.
;; The real reconciliation receipt binds this file to the exact ontology bytes
;; through receipt metadata graph_hash.

(defproblem promote-ZOCEL-016 semantic-jira-workorder
  (:objects
    ZOCEL-016 - workorder
    court-zocel-016 - court
    local-execution-evidence receipt-evidence source-evidence - evidence
    fal-zocel-016-1 fal-zocel-016-2 - falsifier
    cp-zocel-016 - checkpoint)
  (:task (promote ZOCEL-016 ALIVE))
  (:method decompose-promote-ZOCEL-016
    :task (promote ZOCEL-016 ALIVE)
    :subtasks (
      ;; required courts - sj:requiresCourt - stable-key order
      (close-court ZOCEL-016 court-zocel-016) ;; ZOCEL-016 exact-subject court
      ;; required evidence - sj:requiresEvidence - stable-key order
      (produce-evidence ZOCEL-016 local-execution-evidence) ;; Local execution evidence
      (produce-evidence ZOCEL-016 receipt-evidence) ;; Receipt evidence
      (produce-evidence ZOCEL-016 source-evidence) ;; Source evidence
      ;; required receipt classes - sj:requiresReceiptClass - stable-key order
      (record-receipt-class ZOCEL-016 verification)
      ;; falsifiers to discharge - sj:falsifier - stable-key order
      (discharge-falsifier ZOCEL-016 fal-zocel-016-1) ;; Green on mutation
      (discharge-falsifier ZOCEL-016 fal-zocel-016-2) ;; Ad-hoc only
      ;; terminal checkpoint - sj:nextCheckpoint
      (reach-checkpoint ZOCEL-016 cp-zocel-016) ;; ZOCEL-016 checkpoint
      ))
  (:init
    (workorder ZOCEL-016)
    (standing ZOCEL-016 UNKNOWN)
    (base-sha ZOCEL-016 e0a77911b804040d5bd3412a86bf94957ad5a2c6)
    (authority-ceiling ZOCEL-016 CONSTRUCT)
    (exact-subject ZOCEL-016 "zcode-cli:ocel-verify-ci")
    (repository ZOCEL-016 "seanchatmangpt/zcode-cli")
    (replay-identity ZOCEL-016 "semantic-jira:v26.9.22:ZOCEL-016")
    (projection-authority NONE))
  ;; Declared goal, not observed standing: ALIVE with the receipt crown.
  (:goal (and (standing ZOCEL-016 ALIVE) (has-receipt ZOCEL-016))))
