
;; GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-014. DO NOT EDIT.
;;
;; Zero-authority projection: this HDDL file is the promotion plan AS DECLARED
;; by the canonical RDF graph. It executes nothing and holds no authority to
;; actuate, merge, publish, deploy, or promote standing. It is a
;; plan-as-declared projection, not a solved or executed plan: the subtask
;; order below is the declared court / evidence / receipt-class / falsifier /
;; checkpoint structure of the graph in stable-key order and carries no
;; solver-derived ordering. The declared goal follows
;; shapes/work-order.shacl.ttl: standing ALIVE only with the receipt crown.
;; The real reconciliation receipt binds this file to the exact ontology bytes
;; through receipt metadata graph_hash.

(defproblem promote-ZOCEL-014 semantic-jira-workorder
  (:objects
    ZOCEL-014 - workorder
    court-zocel-014 - court
    local-execution-evidence receipt-evidence replay-evidence runtime-evidence source-evidence - evidence
    fal-zocel-014-1 fal-zocel-014-2 - falsifier
    cp-zocel-014 - checkpoint)
  (:task (promote ZOCEL-014 ALIVE))
  (:method decompose-promote-ZOCEL-014
    :task (promote ZOCEL-014 ALIVE)
    :subtasks (
      ;; required courts - sj:requiresCourt - stable-key order
      (close-court ZOCEL-014 court-zocel-014) ;; ZOCEL-014 exact-subject court
      ;; required evidence - sj:requiresEvidence - stable-key order
      (produce-evidence ZOCEL-014 local-execution-evidence) ;; Local execution evidence
      (produce-evidence ZOCEL-014 receipt-evidence) ;; Receipt evidence
      (produce-evidence ZOCEL-014 replay-evidence) ;; Replay evidence
      (produce-evidence ZOCEL-014 runtime-evidence) ;; Runtime evidence
      (produce-evidence ZOCEL-014 source-evidence) ;; Source evidence
      ;; required receipt classes - sj:requiresReceiptClass - stable-key order
      (record-receipt-class ZOCEL-014 replay)
      (record-receipt-class ZOCEL-014 verification)
      ;; falsifiers to discharge - sj:falsifier - stable-key order
      (discharge-falsifier ZOCEL-014 fal-zocel-014-1) ;; Schema-insensitive bridge
      (discharge-falsifier ZOCEL-014 fal-zocel-014-2) ;; Hand-copied schema
      ;; terminal checkpoint - sj:nextCheckpoint
      (reach-checkpoint ZOCEL-014 cp-zocel-014) ;; ZOCEL-014 checkpoint
      ))
  (:init
    (workorder ZOCEL-014)
    (standing ZOCEL-014 UNKNOWN)
    (base-sha ZOCEL-014 e0a77911b804040d5bd3412a86bf94957ad5a2c6)
    (authority-ceiling ZOCEL-014 CONSTRUCT)
    (exact-subject ZOCEL-014 "xaas:ocel-registry-bridge")
    (repository ZOCEL-014 "seanchatmangpt/xaas")
    (replay-identity ZOCEL-014 "semantic-jira:v26.9.22:ZOCEL-014")
    (projection-authority NONE))
  ;; Declared goal, not observed standing: ALIVE with the receipt crown.
  (:goal (and (standing ZOCEL-014 ALIVE) (has-receipt ZOCEL-014))))
