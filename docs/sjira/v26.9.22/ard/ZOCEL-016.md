
<!-- GENERATED by semantic-jira-pack from canonical RDF WorkOrder https://ggen-igniter.dev/ontology/semantic-jira#zocel-016. DO NOT EDIT. -->
# ARD: ZOCEL-016 — ocel-verify-ci — wire scripts/ocel-verify.ts into package.json scripts / CI

scripts/ocel-verify.ts in seanchatmangpt/zcode-cli verifies a recorded .jsonocel plus sibling .receipt.json (generated verifier, table and chain; prints chain-verified and deviations: 0) but is invoked only ad hoc: it appears in no package.json script and no .github/workflows workflow at base e0a77911b804040d5bd3412a86bf94957ad5a2c6. Wire it into package.json scripts / CI so OCEL receipts are verified on every test boundary. Checked-in fixtures exist at test/fixtures/zcode-ocel/ (turn-list.jsonocel, app-server-list.jsonocel, each with a sibling .receipt.json). Standing stays UNKNOWN until a court receipt exists.

## Decision boundary

- **Repository:** seanchatmangpt/zcode-cli
- **Base SHA:** e0a77911b804040d5bd3412a86bf94957ad5a2c6
- **Exact subject:** zcode-cli:ocel-verify-ci
- **Standing:** UNKNOWN
- **Evidence ceiling:** SPECIFIED
- **Authority ceiling:** CONSTRUCT
- **Replay identity:** semantic-jira:v26.9.22:ZOCEL-016
- **Canonical WorkOrder:** https://ggen-igniter.dev/ontology/semantic-jira#zocel-016

This Markdown file is a deterministic projection. It has zero independent actuation,
merge, publication, deployment, or standing-promotion authority. The real reconciliation
receipt binds the projection to the exact ontology bytes through receipt metadata
graph_hash.

## Decision D-1: the boundary

**Decision:** `zcode-cli:ocel-verify-ci` is decided on repository `seanchatmangpt/zcode-cli` at exact
base SHA `e0a77911b804040d5bd3412a86bf94957ad5a2c6`; recorded standing `UNKNOWN`.

**Consequences:** the consequences of this decision are confined to its declared
path scope:


- .github/workflows

- package.json

- scripts

- test/fixtures/zcode-ocel



**Verification:** replay identity `semantic-jira:v26.9.22:ZOCEL-016` re-renders this boundary
byte-identically from the same canonical graph.

## Decision D-2: authority

**Decision:** authority ceiling `CONSTRUCT`; authority requirement:


- NONE



**Consequences:** an authority ceiling outside OBSERVE/SELECT/CONSTRUCT refuses
admission; this record documents the decision and carries no authority of its own.

**Verification:** evidence ceiling `SPECIFIED`; admission is receipted
in the required receipt classes:


- verification



## Decision D-3: promotion

**Decision:** Standing may advance only from an independent exact-head court receipt binding candidateSha, subjectSha and a durable receipt for this subject; a passing test log alone, a generated ticket, or an upstream standing is not promotion.

**Consequences:** promotion is decided only by the required courts:

- **ZOCEL-016 exact-subject court** — Independent court for ocel-verify-ci: runs the acceptance commands at an exact head in the worktree scope and binds candidateSha, subjectSha, tool versions and receipt identity.


**Verification:** the courts accept only the required evidence:

- **Local execution evidence** — Observed repository-local exact-subject execution evidence.

- **Receipt evidence** — Durable receipt evidence for the exact subject and transition.

- **Source evidence** — Exact canonical-source identity evidence.


## Decision D-4: falsifiability

**Decision:** standing `UNKNOWN` holds only while every falsifier of this
decision stays unobserved.

**Consequences:** each falsifier below is an executable refutation condition in the
canonical graph; observing one falsifies this decision.

- **Green on mutation** — The wired verification exiting 0 on a byte-flipped fixture refutes the gate and its coverage claim.

- **Ad-hoc only** — ocel-verify.ts reachable only by manual invocation (absent from package.json scripts and CI workflows) refutes the wiring claim.


**Verification:** each acceptance criterion below is a node in the canonical graph
that must hold for this decision to stand.

- **Baseline verify is green** — cd /Users/sac/dev/zcode-cli && bun scripts/ocel-verify.ts test/fixtures/zcode-ocel/turn-list.jsonocel; echo rc=$?. Expected: rc=0, output contains chain-verified and deviations: 0. Nonzero at base => the subject is BLOCKED before wiring. Earns: BASELINE_GREEN.

- **Wired into a named script and the test boundary** — package.json exposes a script (e.g. ocel:verify) that runs scripts/ocel-verify.ts over the checked-in fixtures, and that script is reachable from the test boundary (composed into test:all or test:unit, or invoked by .github/workflows/ci.yml). grep -c 'ocel-verify' package.json prints >= 1 and the boundary reference is present. Absent => FAIL. Earns: GATE_WIRED.

- **Mutated fixture fails the boundary** — mkdir -p /Users/sac/wt/zocel-runs && cp test/fixtures/zcode-ocel/turn-list.jsonocel /Users/sac/wt/zocel-runs/tamper.jsonocel and printf '\x00' | dd of=/Users/sac/wt/zocel-runs/tamper.jsonocel bs=1 seek=10 conv=notrunc, then run the wired script over the mutated copy: expected nonzero exit; the same script over the untouched fixture exits 0. Both outcomes required. Earns: MUTATION_FAILS_BOUNDARY.

